public class helloName {
    public String helloName(String name) {
        return "Hello " + name +"!"; // returns a string greeting a predefined individual ("name")
    }
// did not think this one would be this easy but ill take it
}
